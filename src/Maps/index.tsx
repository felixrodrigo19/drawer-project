import React from 'react';
import { Text } from 'react-native';

import { Container } from './styles';

const Maps: React.FC = () => {
  return (
    <Container>
      <Text>Maps</Text>
    </Container>
  );
};

export default Maps;
